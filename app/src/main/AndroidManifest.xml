<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gauranggoel.childvacination">

    <application
        android:allowBackup="true"
        android:icon="@drawable/child"
        android:label="@string/app_name"
        android:roundIcon="@drawable/child"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".AddChildDetails" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Authentication" />
        <activity android:name=".VaccineDetails" />
        <activity android:name=".AddDoctorDetail" />
        <activity android:name=".ShowingDoctorDetails" />
        <activity android:name=".EditChildDetails" />
        <activity android:name=".EditDoctorDetails"></activity>
    </application>

</manifest>